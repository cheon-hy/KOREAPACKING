import React from 'react'
import ReactDOM from 'react-dom/client'
import GasketPriceCalculator from './GasketPriceCalculator'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GasketPriceCalculator />
  </React.StrictMode>
)